# Pull Request Description

Add `user-thoughts` skill — a cross-session user decision persistence tool that captures project ideas, design preferences, tech stack choices, and constraints into a project-bound idea repository (mdbase), so knowledge survives across sessions and agent handoffs.

## Change Classification

- [x] Skill PR
- [ ] Docs PR
- [ ] Infra PR

## Issue Link (Optional)

N/A

## Quality Bar Checklist ✅

- [x] **Standards**: I have read `docs/contributors/quality-bar.md` and `docs/contributors/security-guardrails.md`.
- [x] **Metadata**: The `SKILL.md` frontmatter is valid (`name`, `description` <200 chars, `risk`, `source`, `source_repo`, `source_type`, `date_added` all present).
- [x] **Risk Label**: `risk: safe` — reads/writes local files only, no network or destructive operations.
- [x] **Triggers**: `## When to Use` section present with specific triggers: `/user-thoughts`, `/ustht` commands, and natural language in any language when user discusses architecture, tech stack, rules, UI/UX preferences.
- [x] **Limitations**: `## 局限性` section covers: no validation of idea feasibility, sortin dimension classification depends on agent semantic analysis, ignore intervals are context-only, no data sanitization.
- [x] **Security**: Not an offensive skill. No "Authorized Use Only" disclaimer needed.
- [x] **Safety scan**: Skill contains bash commands for directory creation and file operations. All operations restricted to `#ustht/` directory. Path traversal prevention enforced via dimension name validation (`[a-z0-9-]` only). No `curl|bash` or remote fetch patterns.
- [x] **Automated Skill Review**: Will address actionable feedback from `skill-review` GitHub Actions.
- [x] **Manual Logic Review**: Manually reviewed SKILL.md — risk label is `safe`, logic is sound, failure modes covered in `## 常见陷阱` and `## 边界场景`.
- [x] **Local Test**: Skill verified locally with `/ustht init`, `/ustht status`, instant plan recording, and `/ustht sortin`.
- [x] **Repo Checks**: N/A for skill-only PR (no docs/workflows/infra changes).
- [x] **Source-Only PR**: No generated registry artifacts (`CATALOG.md`, `skills_index.json`, `data/*.json`) included.
- [x] **Credits**: Source credit `JularDepick/user-thoughts.SKILL` declared in frontmatter.
- [x] **License provenance**: `license: MIT` and `source_repo: JularDepick/user-thoughts.SKILL` declared in frontmatter. Upstream repo carries MIT license.
- [x] **Maintainer Edits**: Will enable **Allow edits from maintainers** on the PR.

## Screenshots (if applicable)

N/A
